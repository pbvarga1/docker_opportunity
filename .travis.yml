language: python
sudo: false

python:
  - "3.6"

services:
  - docker

stages:
  - tests

install:
  - pip install -r requirements.txt
  - pip install -e app
  - pip install -e web

jobs:
  include:
    - stage: "tests"
      - script: pytest app/tests
      - script: pytest web/tests
